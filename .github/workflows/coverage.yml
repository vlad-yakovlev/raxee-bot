---
name: Coverage

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - run: node -v
      - name: Coverage
        uses: ArtiomTr/jest-coverage-report-action@v2
