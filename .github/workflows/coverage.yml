---
name: PR Checks

on:
  push:
    master
  pull_request:

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Coverage
        uses: ArtiomTr/jest-coverage-report-action@v2
